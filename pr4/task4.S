/*
 * task4.S – Программа суммирования одноцифровых чисел
 *
 * Используя макросы из файла my-macro, эта программа читает символы
 * из стандартного ввода, извлекает одноцифровые числа, суммирует их
 * в 16‑битовом накопителе и выводит результат в десятичной форме.
 * В случае переполнения накопителя выводится соответствующее сообщение.
 *
 * TODO: реализовать алгоритм суммирования и обработки ввода/вывода.
 */

    .include "my-macro"     # подключение макроопределений

    .bss
        sum:    .skip 2      # 16‑битовая область для хранения суммы

    .text
    .global _start

_start:
    # TODO: реализовать чтение символов из stdin, суммирование цифр
    # и проверку переполнения. Используйте макровызовы Getchar, Puts и Exit.
    # При необходимости конвертируйте накопленную сумму в строку для вывода.

    Finish          # завершение программы (макровызов из my-macro)

    .end